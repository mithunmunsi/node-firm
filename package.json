{
  "name": "node-farm",
  "version": "1.0.0",
  "description": "Learning node.js",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"No tests specified\" && exit 0",
    "build": "echo \"Build script is not yet defined.\" && exit 0",
    "deploy": "echo \"Deploy script is not yet defined.\" && exit 0"
  },
  "author": "Mithun Munsi",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "slugify": "~1.3.4"
  },
  "devDependencies": {
    "nodemon": "^1.18.11"
  }
}
